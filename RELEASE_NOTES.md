# 릴리즈 노트 (RELEASE_NOTES.md)

## [v1.0.0.Build.7] - 2026-08-14 11:05:39 (KST)

### 📱 3mm 미세 laser-etched 각인 OCR 줌 엔진 구축
1. **광학/디지털 줌 스캐닝 (1.5x ~ 5.0x)**:
   - 3mm 크기각인을 확대 포착하기 위한 줌 렌즈 및 퀵 버튼 (`[1.5x]`, `[2.5x]`, `[3.5x]`, `[5.0x]`) + 줌 슬라이더 적용.
2. **슬림 마이크로 타겟 레티클 (Micro-Reticle)**:
   - 3mm 각인에 맞춘 슬림형 타겟팅 가이드 레이아웃 적용.
3. **마이크로 돋보기 실시간 미리보기 창 (Magnifier Window)**:
   - 뷰파인더 좌측 상단에 캡처된 3mm 미세 각인 텍스트가 300% 돋보기로 선명하게 확대한 대로 표출.
4. **슈퍼 레졸루션 Canvas 전처리**:
   - 300% 오프스크린 Canvas 확대 + 어댑티브 이진화 선명도 강화 파이프라인.

---

### 🖥️ PC 라벨 프린터 오프셋(Offset) 정밀 교정 & 생산 KPI 화면 전환
1. **생산 KPI 중심 화면 (생산 컨트롤 센터)**:
   - `출력 대기 IMEI 수` | `출력 완료 라벨 수` | `총 수집 수량` KPI 카드 노출.
   - 데이터 테이블은 `[수집 데이터 목록 보기]` 드로어 버튼으로 필요 시에만 개방.
2. **라벨 오프셋 & 위치/크기 정밀 조절 패널 (`LabelOffsetController.jsx`)**:
   - **X / Y 오프셋 위치 이동 (±mm)**: 실물 라벨지 여백 오차 정밀 이동.
   - **글자 크기 (px)**: 폰트 크기 독립 조절.
   - **Code 39 바코드 높이 (mm)**: 바코드 높이 정밀 조절.
3. **`[테스트 라벨 1장 인쇄]` 기능**:
   - 보정한 오프셋 mm 값이 적용된 1 매의 테스트 라벨지를 인쇄하여 사전 오차 교정.
4. **로컬 시드 데이터 자동 로딩 제거**:
   - 메모리 내 임의 시드 데이터 로딩을 전체 제거하고, DB가 비어있을 시 0건으로 처리하여 사용자가 직접 **`[양식 덮어쓰기/업로드]`**를 할 수 있도록 개편.
