# 릴리즈 노트 (RELEASE_NOTES.md)

## [v1.7.0.Build.5] - 2026-08-16 01:34:00 (KST)

### 🧩 [temp_asset 스키마 100% 동적 실시간 렌더링 엔진 탑재] 정규 자산 복사본 탈피 & 완전 자유 스키마 연동

1. **`TempDataTab.jsx`의 고정 15개 헤더 탈피 ➔ `schema_definitions` 실시간 비동기 로드 기반 동적 렌더링**
   - 사용자가 `[스키마 빌더]`에서 정의한 컬럼 개수, 컬럼명, 순서, PK에 따라 테이블 헤더, 엑셀 템플릿, 단건 등록 폼이 100% 실시간 동적 생성
2. **`temp_asset` 초기 기본 스키마를 독립적인 4개 기본 필드(`asset_no(PK)`, `product_name`, `category`, `remark`)로 최적화**

---

## [v1.7.0.Build.4] - 2026-08-16 01:29:00 (KST)

### 🧹 [임의 더미 시드 잔재 자동 청소 및 무허가 더미 삽입 금지 원칙 확립]

1. **브라우저 로컬 스토리지에 남아있던 이전 더미 데이터(`TEMP-2026-001/002`) 자동 소멸 처리**
   - 실제 Supabase `temp_asset` DB 상태(0건)와 100% 일치하도록 순수 빈 테이블(`[]`) 유지
2. **"명시적 지시 없는 임의 더미 데이터 삽입 전면 금지" 원칙 확립**

---

## [v1.7.0.Build.3] - 2026-08-16 01:26:00 (KST)

### 🧹 [하드코딩 더미 시드 전면 삭제 & 자동 검증 스크립트 구축] 순수 사용자 스키마 기반 운영

1. **`TempDataTab.jsx`의 하드코딩 더미 샘플 데이터 주입 코드 완전 삭제**
   - 사용자가 스키마를 편성하기 전 불필요한 오류를 유발하지 않도록 초기 상태를 순수 빈 데이터(`[]`)로 완전 전환
   - 엑셀 양식 다운로드 시에도 더미 데이터 행을 제거하고 스키마 정의 헤더만 갖춘 순수 템플릿 생성
2. **배포 전 UI 렌더링 무결성 자동 전수 검증 스크립트(`scripts/verify_ui_render.mjs`) 구축 및 파이프라인 영구 결합**
   - 전 21개 React 컴포넌트의 임포트/식별자 무결성 100% 자동 검증 통과

---

## [v1.7.0.Build.2] - 2026-08-16 01:20:00 (KST)

### 🐛 [런타임 렌더링 긴급 패치] `FolderOpen` 아이콘 누락 임포트 수정

1. **`PCDashboardView.jsx`의 `FolderOpen` 아이콘 임포트 누락 즉시 수정**
   - 시스템 렌더링 복구 에러(`FolderOpen is not defined`) 완벽 해결

---

## [v1.7.0.Build.1] - 2026-08-16 01:18:00 (KST)

### 🌟 [temp_asset 임시 데이터 관리 신설 & 스키마-엑셀-디자인-즉시출력 4단계 라이프사이클 완비]

1. **신규 탭 `[임시 데이터 관리]` (`temp_asset`) 전격 신설**
   - 현재 정의된 스키마 필드 기반 동적 데이터 그리드 테이블 표출
   - `[엑셀 양식 다운]`: 현재 스키마 맞춤형 .xlsx 엑셀 템플릿 즉시 생성 및 다운로드
   - `[엑셀 대량 업로드]`: 엑셀 시트 파싱 ➔ `temp_asset` 테이블 실시간 대량 벌크 주입(Bulk Upsert)
   - 단건 등록/수정/삭제 모달, `[전체 비우기(초기화)]`, `[선택 항목 즉시 라벨 인쇄]`
2. **`[스키마 빌더]` 비밀번호 보호 해제 (누구나 자유 진입)**
   - `temp_asset` 전용 커스텀 필드(열)를 누구나 실시간 빌드 및 저장
3. **라벨 즉시 출력(`DirectPrintTab`) 및 디자인 연동 완비**
   - PK 스캔 시 `temp_asset` 테이블에서 즉시 조회하여 전용 디자인 라벨 1초 즉시 출력

---

## [v1.6.0.Build.38] - 2026-08-16 00:48:00 (KST)

### 🎯 [시각적 디자인 캔버스 & 물리적 ZPL 보정 분리 원칙 확립] 라벨 기준 디자인 무결성 보존

1. **캔버스 화면(디자이너 및 미리보기) 렌더링 시 물리적 보정값(offsetX, offsetY) 전면 제외**
   - 캔버스 화면은 항상 순수한 라벨 용지 기준 디자인 좌표(`xMm`, `yMm`)만으로 정밀 렌더링되어 사용자가 시각적으로 직관적인 디자인을 상시 유지
2. **물리적 보정값(`offsetX`, `offsetY`)은 실제 프린터로 전송되는 ZPL 명령어 생성 시에만 100% 합산 반영**
   - 대형 오프셋(+20mm 등)을 설정해도 캔버스가 찌그러지거나 라벨 밖으로 삐져나가지 않고, 실물 인쇄 시에만 정확한 물리 위치로 보정 출력

---

## [v1.6.0.Build.37] - 2026-08-16 00:28:00 (KST)

### 💾 [`^LH` 및 `^PW` 사용 여부 서식 DB 영구 보존 완비] 캔버스 및 실시간 출력 100% 연동

1. **디자인 서식의 `^LH` (원점) 및 `^PW` (인쇄폭) 토글 상태가 서식 저장 시 Supabase DB 및 로컬에 영구 보존**
2. **실시간 바코드 스캔 출력(`DirectPrintTab`) 시에도 해당 서식의 `^LH`/`^PW` 설정값 1:1 자동 적용 완비**

---

## [v1.6.0.Build.36] - 2026-08-16 00:27:00 (KST)

### 🎚️ [`^LH` 및 `^PW` 개별 ON/OFF 토글 스위치 탑재] 2개 위치(용지 패널 & ZPL 에디터 상단) 완비

1. **좌측 [용지 규격] 패널에 `[^LH (원점)]`, `[^PW (인쇄폭)]` 체크박스 신설**
   - 용지 규격 입력 바로 아래에서 원점 자동 정렬 및 인쇄폭 선언의 포함 여부를 자유롭게 On/Off
2. **중앙 [ZPL 코드 직접 편집기] 상단 바에도 `[^LH]`, `[^PW]` 즉시 토글 배치**
   - ZPL 코드를 보면서 원클릭으로 `^LH`와 `^PW`를 켜고 끄며 실시간 ZPL 갱신 및 즉시 테스트 인쇄 지원
3. **서식 DB 및 로컬 스토리지에 무누락 영구 저장**

---

## [v1.6.0.Build.35] - 2026-08-16 00:24:00 (KST)

### ✍️ [ZPL 직접 편집 & 즉시 테스트 인쇄 에디터 탑재] 실시간 수동 ZPL 튜닝 환경 완비

1. **디자이너 하단 ZPL 영역을 '직접 편집 가능한 에디터(Textarea)'로 전격 개편**
   - 사장님께서 ZPL 코드를 에디터에서 직접 수정(예: `^FO240,15`, `^BQN,2,3` 등) 가능
2. **`[수정된 ZPL로 즉시 테스트 인쇄]` 버튼 신설**
   - 에디터에서 수정한 ZPL을 즉시 실물 프린터로 원클릭 1초 만에 직접 쏴서 최적의 좌표와 파라미터를 실시간 검증
3. **`[자동생성 코드로 복원]` 및 `[복사]` 기능 지원**
   - 캔버스 설정 기준 코드로 언제든 원클릭 초기화 가능

---

## [v1.6.0.Build.34] - 2026-08-16 00:12:00 (KST)

### 🎛️ [현장 실물 프린터 적응형 보정 체계] `^LH`/`^PW` 롤백 및 음수/양수 자유 보정(`[-]`/`[+]` 버튼) 완비

1. **`^LH` 및 `^PW` 강제 명령어를 ZPL 헤더에서 전면 제거**
   - 실물 프린터 펌웨어 및 드라이버의 여백 충돌 방지를 위해 기존 순수 헤더(`^XA^MD21...`)로 롤백
2. **모든 디자인 요소에 음수(Negative) 보정값 입력 및 `[-]` `[+]` 0.5mm 증감 컨트롤 탑재**
   - `-100mm ~ +100mm` 범위에서 자유롭게 좌/우/상/하로 밀어 실물 프린터의 물리적 오차를 현장 맞춤형으로 100% 극복

---

## [v1.6.0.Build.33] - 2026-08-15 23:52:00 (KST)

### 🏛️ [Zebra 공식 표준 규약 완비] 용지 규격별 `^LH`(라벨 원점) 및 `^PW`(인쇄폭) 자동 중앙 정렬 탑재

1. **Zebra 표준 `^LH` (Label Home) 자동 계산 엔진 탑재**
   - 4인치(104mm = 832 dots) 헤드 기준, 용지 폭에 따른 물리적 좌측 여백을 자동 계산하여 라벨 정중앙 정렬
   - 24mm 소형 라벨 ➔ `^LH320,0`, `^PW192`
   - 72mm 라벨 ➔ `^LH128,0`, `^PW576`
2. **라벨 디자이너 좌측 용지 규격 패널에 실시간 `^LH`/`^PW` 계산 상태 표출**
   - 캔버스의 `(0,0)`이 실물 라벨의 `(0,0)`과 100% 일치하도록 보장

---

## [v1.6.0.Build.32] - 2026-08-15 23:48:00 (KST)

### 📐 [개별 객체 인쇄 미세 보정 시스템] 전 디자인 요소 X/Y 오프셋(보정값 mm) 완비

1. **모든 디자인 요소(헤더 텍스트, 바코드/QR, 추가 텍스트, 이미지, 구분선)에 X/Y 인쇄 보정(mm) 속성 신설**
   - QR 코드 등 프린터 기종/엔진별 좌표 오차가 발생하는 요소를 0.5mm/0.1mm 단위로 정밀 보정
2. **ZPL 생성 및 캔버스 렌더링에 실시간 합산 좌표 적용**
   - 최종 인쇄 Dot 및 캔버스 화면: `(기본 좌표 + 보정값) × 배율`로 실시간 완벽 동기화

---

## [v1.6.0.Build.31] - 2026-08-15 23:30:00 (KST)

### 🏷️ [QR ZPL 규격 일치화] `^FDLA` (초고밀도 소형화 모드) 실무 C# 원본 규격 완벽 일치

1. **QR 코드 ZPL 데이터 접두사를 `^FDLA`로 공식 전환**
   - `L` (Low, 7% 오류정정): QR 코드 격자를 최소화하여 소형 라벨(24×15mm)에서 초고밀도/최대 선명도로 인쇄
   - `A` (Automatic): 데이터 문자열 자동 포맷팅

---

## [v1.6.0.Build.30] - 2026-08-15 23:23:00 (KST)

### 📱 [QR 코드 크기 조절 완비] 배율(1~10) 조절 슬라이더 & 캔버스 실물 비례 벡터 렌더링 탑재

1. **라벨 디자이너 속성 패널에 QR 코드 전용 크기(배율: 1~10) 조절 UI 신설**
   - 슬라이더, `[-]`/`[+]` 버튼, 실물 크기(mm) 자동 환산 가이드 제공
2. **라벨 캔버스 및 출력 미리보기 캔버스 실물 1:1 비례 정밀 렌더링**
   - 배율 변경 시 캔버스 상의 QR 코드가 실물 크기 비례(`^BQN,2,{qrScale}`)로 실시간 반응
   - ZPL 출력 시 실물 Zebra 프린터와 100% 동일한 크기로 인쇄

---

## [v1.6.0.Build.29] - 2026-08-15 23:11:00 (KST)

### 🎯 [타겟 프린터 DB 무누락 영구 저장 & 디자이너 서식 선택기 탑재]

1. **`targetPrinterId` & `targetPrinterName`이 DB `paper` 컬럼에 100% 정상 저장되도록 구조 완비**
   - 템플릿 최상위 및 `paper` 내부 양방향 정규화(`normalized`)를 통해 공백 저장 버그 완전 박멸
2. **라벨 디자이너 상단 바에 `[서식 즉시 전환]` 드롭다운 신설**
   - 서식을 바꿀 때마다 해당 서식의 지정 프린터 정보를 즉시 확인하고 바로 수정/저장 가능
3. **실시간 출력 화면 (`DirectPrintTab`) 서식 선택 시 전용 프린터 0.001초 즉시 동기화 보장**
   - `id`, `name`, `rawName`, 부분 문자열 4중 유연 매칭으로 어떤 환경에서도 정확하게 매핑

---

## [v1.6.0.Build.28] - 2026-08-15 23:03:00 (KST)

### 🛠️ [서식-프린터 DB 동기화 완전 수정] 온라인 DB 저장/조회 누락 해결 & 서식 선택 시 프린터 100% 즉시 전환

1. **온라인 DB (`label_templates`) 저장 및 로드 시 `targetPrinterId`/`targetPrinterName` 누락 버그 해결**
   - `saveBackendLabelTemplate`, `syncTemplatesWithBackend`, `fetchBackendLabelTemplate` 전 과정에 프린터 메타데이터 완벽 보존
2. **실시간 출력 화면 (`DirectPrintTab`) 서식 선택 시 프린터 모델 즉시 자동 전환**
   - `id`, `name`, `rawName` 3중 정밀 매칭을 통해 서식 변경 즉시 연결된 전용 프린터로 완벽 전환 & 에이전트 동기화

---

## [v1.6.0.Build.27] - 2026-08-15 22:37:00 (KST)

### 🔒 [서식-프린터 1:1 바인딩] 디자인 단계 프린터 지정 & 실시간 출력 프린터 오설정 완전 방지

1. **라벨 서식 디자인 단계에서 대상 프린터 1:1 지정 바인딩**
   - 로컬에 설치된 실제 Zebra 프린터 목록을 조회하여 각 서식별로 출력할 전용 프린터를 지정 및 저장
2. **실시간 출력 화면 (`DirectPrintTab`) 프린터 수동 변경 제거 & 서식 자동 바인딩**
   - 실시간 화면에서 작업자가 임의로 프린터를 변경하여 용지를 낭비하던 문제를 원천 방지
   - 서식 선택 시 해당 서식에 지정된 전용 프린터로 에이전트 설정 및 출력이 100% 자동 전환

---

## [v1.6.0.Build.26] - 2026-08-15 22:26:00 (KST)

### 🖨️ [인코딩 원천 해결] `iconv-lite` 기반 순수 CP949(EUC-KR) 바이너리 바이트 직통 전송

1. **Zebra ^CI26 전용 완성형 한글 2바이트(CP949) 스트림 탑재**
   - Node.js 에이전트에 `iconv-lite`를 설치하여 한글 ZPL을 100% 무결한 CP949 바이트 버퍼로 직접 변환
   - winspool RAW 및 TCP 포트로 CP949 바이트 스트림을 직통 전송하여, 한글 폰트(`KFONT3.FNT` / `UHANGUL.DAT`)가 100% 정상 인식/인쇄되도록 완성

---

## [v1.6.0.Build.25] - 2026-08-15 22:18:00 (KST)

### ⚡ [스캐너 엔터 이벤트 인터셉트 원천 차단 & 100% 즉시 수신 보장]

1. **`hardwareScanner` 전역 리스너의 입력창 엔터 키 차단(`stopPropagation`) 버그 완전 박멸**
   - 텍스트 입력창 포커스 시 전역 리스너가 엔터 키 이벤트를 가로채 죽이던 구조적 결함 제거
2. **`DirectPrintTab` 입력 폼 `<form onSubmit>` + `<input onKeyDown>` 2중 방어선 완비**
   - 스캐너의 고속 엔터 신호를 0.001초 만에 100% 확실하게 수신하여 즉시 인쇄 집행

---

## [v1.6.0.Build.24] - 2026-08-15 22:15:00 (KST)

### 🏷️ [스마트 헤더 분기] 소형 단순 QR(^XA^MD21) & 대형 한글 라벨 헤더 완벽 지원

1. **라벨 내용에 따른 ZPL 헤더 지능형 자동 분기**
   - 한글 포함 라벨: `^XA^MD21^BY2,2.0^FS^SEE:UHANGUL.DAT^FS^CW1,E:KFONT3.FNT^CI26^FS`
   - 한글 미포함 소형 단순 QR / 영문 라벨: `^XA^MD21` (초경량 초고속 헤더)

---

## [v1.6.0.Build.23] - 2026-08-15 22:13:00 (KST)

### 🖨️ [한글 ZPL 헤더 & CP949 바이트 인코딩 전송 완벽 일치화]

1. **ZPL 헤더 C# 원본 문자열 100% 일치**
   - `^XA^MD21^BY2,2.0^FS^SEE:UHANGUL.DAT^FS^CW1,E:KFONT3.FNT^CI26^FS`
2. **에이전트 `^CI26` CP949(EUC-KR) 바이트 변환 전송 탑재**
   - Windows winspool RAW 전송 시 `^CI26` 선언에 맞춰 2바이트 CP949 완성형 인코딩으로 변환하여 프린터에 전송, 한글 깨짐/누락 원천 박멸

---

## [v1.6.0.Build.22] - 2026-08-15 21:58:00 (KST)

### 📐 [ZPL 폰트 크기 1:1 직관 매핑 & 실무 ZPL 헤더 원형 유지]

1. **UI 설정값 = ZPL 폰트 크기 1:1 완벽 일치화**
   - UI에서 폰트 크기를 `25`로 설정하면 ZPL에서도 `^A1N,25,25` (또는 `^A0N,25,25`)로 배율 왜곡 없이 1:1 직관 생성
   - 캔버스 화면 렌더링도 203 DPI 용지(1mm=8dots) 대비 1:1 정밀 비율로 동기화
2. **실무 정통 ZPL 헤더 원형 그대로 유지**
   - `^XA` / `^MD21^LH0,0` / `^SEE:UHANGUL.DAT^FS` / `^CW1,E:KFONT3.FNT^FS` / `^CI26^FS` 원형 보존

---

## [v1.6.0.Build.21] - 2026-08-15 21:50:00 (KST)

### 🖨️ [UBUS 정통 한글 ZPL 엔진 탑재 & 바코드 1샷 100% 즉시 인쇄 보장]

1. **기존 UBUS 실무 검증 네이티브 ZPL 엔진으로 전면 전환**
   - Zebra 프린터 한글 폰트 규격(`^SEE:UHANGUL.DAT^FS^CW1,E:KFONT3.FNT^CI26^FS`) 100% 탑재
   - 한글 텍스트는 `^A1N,{h},{w}`로, 영문은 `^A0N,{h},{w}`로 크기(12pt ➔ 30 dots ≈ 3.75mm) 완벽 스케일링
   - 글자가 1mm로 찌그러지거나 한글 폰트가 죽는 문제 완전 해결
2. **바코드 스캐너 1샷 1출력 씹힘 현상 원천 해결**
   - 엔터 이벤트 시 `e.currentTarget.value`를 즉시 넘겨 비동기 레이스 컨디션 제거
   - DB에 미등록된 바코드 번호도 기본 라벨로 즉시 생성하여 100% 무조건 인쇄 집행

---

## [v1.6.0.Build.20] - 2026-08-15 21:34:00 (KST)

### 🛡️ [시스템 안정성] React Error #31 (Error 객체 직접 렌더링) 완전 해결

1. **`ErrorModal` 및 `App.jsx` 에러 메시지 문자열(String) 안전 변환 방어벽 구축**
   - 하드웨어 통신 또는 렌더링 예외 발생 시 `Error` 객체가 JSX 노드로 직접 전달되어 화면 전체가 크래시(`Minified React error #31`)되던 문제 완전 해결
2. **`DirectPrintTab` WYSIWYG 비트맵 ZPL 생성 2중 fallback 안전망 적용**

---

## [v1.6.0.Build.19] - 2026-08-15 21:20:00 (KST)

### 🖨️ [인쇄 파이프라인 단일화] 1회 출력 명령 = 1회 ZPL 전송 완벽 보장 & 큐 PENDING 중복 제거

1. **`insertPrintQueue`의 강제 `PENDING` 하드코딩 버그 완전 박멸**
   - 직접 출력 시 `status: 'COMPLETED'`로 넘겨도 DB에 무조건 `PENDING`으로 저장되어, 에이전트의 실시간 큐 리스너가 2번째 더미 라벨을 또 출력하던 근본 원인 해결
   - 이제 직접 출력 시 `print_status: 'PRINTED'`로 INSERT되어 에이전트가 중복 인쇄하지 않고 1회 출력만 무결하게 수행됨
2. **모든 뷰(`DirectPrintTab`, `LabelDesignerTab`) 캔버스 폰트 크기 계산식 100% 통일**

---

## [v1.6.0.Build.18] - 2026-08-15 21:15:00 (KST)

### 📐 [캔버스 폰트 비율 혁신] 물리적 Pt ➔ mm(0.3528mm) 정밀 렌더링 공식 복구

1. **캔버스 폰트 크기 왜곡 원인 완전 해결**
   - 기존 캔버스 렌더러에 적용되어 있던 인위적 축소 계수(`* 0.46`)를 제거하고, 국제 표준 물리 공식 `1pt = 25.4/72 mm (약 0.3528mm)`를 1:1로 적용
   - 이제 72×40mm 용지에서 12pt, 14pt, 20pt, 60pt의 글자 크기 비율이 화면 캔버스와 실물 출력물에서 100% 정밀하게 일치함
2. **비트맵 렌더러(ZPL ^GFA)와 캔버스 UI 폰트 비율 1:1 완벽 동기화**

---

## [v1.6.0.Build.17] - 2026-08-15 21:03:00 (KST)

### 🖨️ [인쇄 정밀도 혁신] 100% WYSIWYG 비트맵(^GFA) 렌더러 탑재 & 중복 인쇄(테스트라벨) 완전 박멸

1. **테스트 라벨 중복 인쇄(2장 출력) 원인 완전 박멸**
   - `sendZplToPrinter`에서 고정 더미 테스트 라벨을 쏘던 `/api/test-print` 호출을 전면 제거하고, 실제 생성된 ZPL을 쏘는 **`POST /api/print-direct` 직통 단일 파이프라인으로 일원화** (단 1장만 정상 출력)
2. **한글 깨짐 & 캔버스 불일치 완전 해결 (100% WYSIWYG `^GFA` 그래픽 엔진)**
   - 프린터 한글 폰트 미설치 시 텍스트가 깨지거나 왜곡되던 문제를 해결하기 위해, HTML5 Canvas에서 203 DPI로 정밀 렌더링한 후 Zebra ZPL II `^GFA` 비트맵으로 변환하여 전송
   - **화면 캔버스에 보이는 한글 폰트, 바코드 크기, 줄 간격, 로고 이미지가 100.0% 오차 없이 실물 라벨에 그대로 인쇄됨**
3. **에이전트 Windows RAW 전송 UTF-8 인코딩 복구**
   - `winspool RAW` 버퍼를 `ascii` ➔ `utf-8`로 수정하여 데이터 손실 방지

---

## [v1.6.0.Build.16] - 2026-08-15 20:45:00 (KST)

### 🪟 [에이전트 UX 혁신] 설정 정상 시 콘솔 자동 숨김(Hidden) & 프론트엔드 웹 화면 자동 실행 탑재

1. **설정값(env) 정상 로드 시 콘솔창 자동 Hidden 처리 (`ShowWindow(hWnd, SW_HIDE)`)**
   - 환경변수 및 프린터 설정이 정상 로드되면 1.2초 후 까만 콘솔창이 자동으로 숨겨져 백그라운드 무인 실행 모드로 전환
   - 설정 누락 또는 초기화 실패 시에만 콘솔창을 그대로 유지하여 에러 원인 표출
2. **에이전트 로컬 UI(127.0.0.1:9988) 대신 실제 프론트엔드 URL(`https://dragonrpa.github.io/ImageScan/`) 1회 자동 실행**
   - 에이전트 실행 시 불필요한 로컬 개발 화면 대신 실제 작업 화면이 기본 브라우저로 쾌속 실행됨

---

## [v1.6.0.Build.14] - 2026-08-15 20:24:00 (KST)

### 📥 [에이전트 다운로드 404 해결] `.gitignore` dist 예외 처리 및 GitHub Pages 직통 에셋 탑재

1. **`.gitignore` 수정 (`/dist/` 루트만 차단, `!print-agent/dist/` 예외 허용)**
   - GitHub 원격 저장소에 `UBUS_DragonRPA_Agent.exe` 바이너리가 누락되어 404 에러가 발생하던 원인 완벽 해결
2. **`public/UBUS_DragonRPA_Agent.exe` 웹 정적 에셋 동시 배포**
   - GitHub Pages 사이트 직통 경로(`https://dragonrpa.github.io/ImageScan/UBUS_DragonRPA_Agent.exe`)로 0.1초 만에 404 없는 안정적 다운로드 보장

---

## [v1.6.0.Build.13] - 2026-08-15 20:21:00 (KST)

### 🤖 [에이전트 버전관리] 더미 제거 & GitHub 원격 `version.json` 실시간 조회 및 CORS Preflight 완비

1. **에이전트 원격 실시간 버전 SSOT 파이프라인 구축 (`print-agent/version.json`)**
   - 하드코딩된 더미 버전 대신 GitHub 원격 `version.json` 메타데이터를 실시간 비동기 조회하여 실제 배포 버전과 1:1 비교
2. **로컬 에이전트 CORS Preflight (`OPTIONS` 204) 및 Private Network Access (PNA) 완벽 지원**
   - 브라우저 HTTPS 환경에서 로컬 에이전트 HTTP 호출 시 발생하던 `Failed to fetch` 통신 차단 이슈 완벽 해결
3. **업데이트 모달 내 실제 릴리즈 노트 표출 및 최신 바이너리 직접 다운로드 링크 제공**

---

## [v1.6.0.Build.12] - 2026-08-15 20:17:00 (KST)

### 🧹 [로컬 시드 완전 박멸] 로컬 하드코딩 시드 데이터 전면 제거 & 100% DB SSOT 확립

1. **라벨 서식 관련 로컬 시드(`BUILTIN_PRESETS`) 및 주입 코드 전면 영구 삭제**
   - 클라이언트 소스코드 내 하드코딩된 더미 서식 목록을 100% 제거
   - `syncTemplatesWithBackend()`가 오직 Supabase `label_templates` 테이블에서 조회된 실제 온라인 DB 레코드만을 렌더링하고 동기화하도록 단일 진실의 원천(SSOT) 원칙 완결

---

## [v1.6.0.Build.11] - 2026-08-15 20:15:00 (KST)

### ☁️ [라벨 서식 DB 저장] Supabase `label_templates` 스키마 정합성 및 저장 오류 수정

1. **Supabase `label_templates` 저장 페이로드 스키마 정합성 보정 (`saveBackendLabelTemplate`)**
   - DB 테이블에 없던 `target_table` 단독 컬럼 대신 `paper: { ...paper, targetTable }` 내장 및 `schema_id: null`로 스키마 캐시 불일치 에러(`PGRST204`)를 완벽히 해결
2. **빌트인 기본 서식 3종 Supabase 원격 DB 시딩 완료**
   - `tpl_asset_default_72x40`, `tpl_asset_compact_50x30`, `tpl_temp_asset_default_72x40` 원격 DB 적재 완료
3. **디자이너 화면에서 서식 저장 시 성공/실패 명확한 피드백 반영**

---

## [v1.6.0.Build.10] - 2026-08-15 20:08:00 (KST)

### ⚡ [블루투스 스캐너] 슬립 고스트 세션 1초 강제 리셋 & 윈도우 원클릭 재연결 완비

1. **에이전트 블루투스 스택 1초 강제 리셋 엔진 구현 (`POST /api/bluetooth/reconnect`)**
   - 장시간 미사용으로 스캐너가 슬립 모드에 빠져 윈도우와 페어링 불일치(고스트 세션) 발생 시, 버튼 클릭 한 번으로 Windows Bluetooth 스택을 즉시 리프레시하여 스캐너 방아쇠 1회 클릭 시 즉시 재연결되도록 완벽 해결
2. **`[⚡ 스캐너 1초 재연결]` & `[⚙️ 블루투스 설정]` 원클릭 버튼 탑재**
   - 번거롭게 윈도우 설정 창을 뒤적거릴 필요 없이 웹 화면에서 1초 만에 스캐너 연결을 즉시 복구

---

## [v1.6.0.Build.9] - 2026-08-15 19:56:00 (KST)

### 🏛️ [라벨 서식 SSOT] 서버 DB (`label_templates`) 절대 단일 기준(Server-First) 확립

1. **라벨 디자인은 서버 DB에 저장된 서식을 100% 무조건 가져다 사용하는 원칙 확립**
   - 사장님/관리자가 서버에 저장한 라벨 서식이 전사의 단일 진실의 원천(SSOT)으로 작동하며, 모든 작업자 PC가 서버의 최신 서식을 실시간 로드하여 사용
   - 하드웨어(프린터/블루투스)는 로컬 독립 기억, 디자인 서식은 서버 단일 진실의 원천으로 역할과 책임(R&R) 엄격 분리

---

## [v1.6.0.Build.8] - 2026-08-15 19:55:00 (KST)

### 💾 [프린터 로컬 독립 기억] 활성 프린터 선택 시 로컬 PC 2중 영구 기억 연동

1. **프린터 선택 시 브라우저 `localStorage` + 에이전트 `agent-config.json` 동시 영구 저장**
   - 각 작업 컴퓨터(A PC, B PC)마다 선택한 프린터(ZT411, ZD420 등)를 로컬에 격리 저장하여 다른 컴퓨터와 간섭 없이 독립 유지
   - 익일 재접속 시에도 내가 선택했던 프린터로 100% 자동 복원

---

## [v1.6.0.Build.7] - 2026-08-15 19:54:00 (KST)

### 📡 [블루투스 스캐너] 라벨 즉시 출력 탭 원클릭 빠른 페어링 & Zero-Focus 무인 감지 탑재

1. **Web Bluetooth 바코드 스캐너 직접 연결 매니저 구현 (`bluetoothScannerManager.js`)**
   - 브라우저에서 원클릭으로 블루투스 바코드 스캐너를 검색/페어링하고 배터리 잔량 및 연결 상태를 실시간 관리
2. **`DirectPrintTab` 스캔창 상단에 `[📡 블루투스 스캐너 빠른 연결]` 버튼 및 실시간 상태 배지 탑재**
   - 연결 완료 시 `🟢 [기기명] 연결됨` 배지 표출 및 스캔 즉시 0.1초 자동 출력 연동
3. **Zero-Focus 무인 감지 가속 엔진 완비**
   - 마우스 포커스가 입력창 밖에 있더라도 블루투스 바코드 스캐너 입력을 60ms 이내에 가로채서 즉시 자동 출력 집행

---

## [v1.6.0.Build.6] - 2026-08-15 19:52:00 (KST)

### 🖨️ [프린터 연결] 로컬 PC 연결 실제 Zebra/Windows 프린터 실시간 스캔 & 자동 감지 탑재

1. **실제 하드웨어 프린터 자동 스캔 엔진 구현 (`fetchActualConnectedPrinters`)**
   - 로컬 에이전트(`http://127.0.0.1:9988/api/printers`)와 연동하여 Windows OS에 실제로 연결된 프린터(ZDesigner ZT411, ZD420, USB001, LAN IP 등)를 실시간 스캔
2. **`DirectPrintTab` 마운트 시 실물 프린터 자동 로드 & `[🔄 실제 프린터 검색]` 원클릭 버튼 완비**
   - 하드코딩된 더미 목록 대신 실제 컴퓨터에 연결된 프린터가 `🟢` 배지와 함께 드롭다운에 즉시 표시됨

---

## [v1.6.0.Build.5] - 2026-08-15 19:50:00 (KST)

### ☁️ [라벨 서식 디자인] 온라인 DB (`label_templates` 테이블) 전체 서식 실시간 자동 동기화 탑재

1. **온라인 DB ➔ 로컬 실시간 서식 동기화 엔진 구현 (`syncTemplatesWithBackend`)**
   - 기존 로컬 스토리지에만 머무르던 한계를 해결하고, 화면 접속 시 Supabase `label_templates` 테이블의 전체 서식 목록을 즉시 조회하여 드롭다운 프리셋 목록에 자동 병합 및 갱신
2. **`LabelDesignerTab` 및 `DirectPrintTab` 마운트 시 실시간 동기화**
   - 어느 PC나 모바일에서 접속하더라도 온라인 DB에 등록된 최신 라벨 서식 목록이 즉시 표출됨
3. **`saveBackendLabelTemplate` UPSERT 정합성 강화**
   - 서식 저장 시 `target_table`, `is_default`, `elements`, `paper` 등이 온라인 DB에 1원/1필드도 오차 없이 저장 및 전사 실시간 공유

---

## [v1.6.0.Build.4] - 2026-08-15 19:42:00 (KST)

### 🤖 [스마트 자가 업데이트] 프론트엔드 실시간 감지 & 에이전트 원클릭 Self-Update 워치독 탑재

1. **에이전트 자가 교체 워치독 엔진 구현 (`print-agent/zebra-agent.cjs`)**
   - `POST /api/self-update`: GitHub Releases에서 최신 `UBUS_DragonRPA_Agent.exe`를 다운로드하여 `updater.bat` 워치독을 통해 기존 실행 중인 프로세스를 1초 만에 안전하게 자동 교체 및 재시작
   - 기존 `agent.env` 및 `agent-config.json`의 프린터 IP/포트 설정 100% 무손실 유지
2. **프론트엔드 실시간 에이전트 상태/버전 감지 매니저 (`agentUpdateManager.js`)**
   - 로컬 에이전트 실행 여부 및 버전(`v1.4`)을 10초 주기로 실시간 모니터링
   - 구버전 실행 감지 시 상단 헤더에 `[⚠️ 에이전트 업데이트 (v1.3 ➔ v1.4)]` 배지 노출
3. **원클릭 스마트 자가 업데이트 모달 (`AgentUpdateModal.jsx`)**
   - 작업자가 버튼 클릭 한 번으로 새 에이전트 다운로드 ➔ 프로세스 교체 ➔ 자동 재접속을 3초 만에 원스톱 처리

---

## [v1.6.0.Build.3] - 2026-08-15 19:35:00 (KST)

### 🔒 [DB 연결] Supabase 핵심 연결 정보 2종 소스 내장 영구 기억 및 자동 치유 완비

1. **DB 핵심 정보 2종 빌트인 내장 및 영구 기억 (`supabaseClient.js`, `.env`)**
   - `URL`: `https://tfgbpgutxxlhqbzewkyt.supabase.co`
   - `Anon Key`: `sb_publishable_wruJQfp3Op-ISvVwb4ZdmA_2OqMUJeQ`
   - 브라우저 캐시 삭제, 시크릿 모드, 다른 PC 최초 접속 시에도 수동 입력 없이 **100% 자동 DB 연결됨 상태로 시작**
2. **URL 오타 자동 치유 (Auto-Healing) 엔진 탑재**
   - 구 오타 URL(`tfgbpgutxxlhqbzewky.supabase.co`)이 남아있더라도 올바른 정규 URL로 실시간 자동 교정

---

## [v1.6.0.Build.2] - 2026-08-15 19:26:00 (KST)

### 🐛 [라벨 즉시 출력] DB 실시간 단건 조회 대상 테이블 정합성 수정 (`asset` / `temp_asset` 직통 쿼리)

1. **DB 조회 대상 테이블 불일치 버그 긴급 조치 (`DirectPrintTab.jsx`)**
   - 원인: `데이터 목록` 탭은 Supabase `asset` 정규 마스터 테이블을 조회하는데, `라벨 즉시 출력` 탭이 `scan_records` 레거시 테이블을 조회하여 시리얼(`TVK73P6QKN`) 등의 정상 자산을 미조회 처리하던 문제.
   - 수정: `targetTable === 'asset'`일 때 Supabase `asset` 테이블에서 `asset_no`, `serial_no`, `imei`를 직통 정밀/부분일치 조회하도록 수정. `temp_asset` 역시 `temp_asset`/`temp_assets` 마스터 테이블을 최우선 조회하도록 100% 동기화.

---

## [v1.6.0.Build.1] - 2026-08-15 19:10:00 (KST)

### 🚀 [라벨 즉시 출력] 메인 랜딩 탭 신설 및 초고속 스캔-즉시 인쇄 워크스테이션 구축

1. **📑 첫 번째 메인 랜딩 탭 신설 (`DirectPrintTab.jsx`)**
   - 시스템 접속 시 실무자가 가장 먼저 사용하는 `라벨 즉시 출력`을 최우선 기본 탭으로 배치
2. **🎛️ 상단 3대 환경 설정 영역**
   - **프린터 선택 & 등록**: Zebra WebSerial, WebUSB, Zebra Browser Print, 네트워크 Raw IP 등록/선택
   - **라벨 디자인 서식 선택**: 저장된 모든 라벨 서식 목록 중 선택
   - **타겟 DB 선택**: `asset` (자산관리) vs `temp_asset` (임시자산) 토글
3. **⚡ 초고속 바코드 스캔 입력 & 0클릭 즉시 자동 라벨 출력**
   - 대형 단일 입력창에 자산번호, 시리얼, 임시자산 식별자를 바코드 스캐너로 스캔(Enter) 즉시 타겟 DB 실시간 조회
   - 조회 결과 발견 시 선택된 디자인 서식대로 프린터에서 **즉시 자동 출력(Direct Print)** 집행!
   - 화면에 1:1 정밀 라벨 캔버스 표출, 성공 피드백, 최근 출력 이력 1줄 누적
   - 입력창 자동 비움 및 포커스 유지로 연속 스캔 작업 최적화
4. **📜 최근 즉시 출력 이력 (Today's Scan & Print Logs) 테이블 완비**

---

## [v1.5.0.Build.8] - 2026-08-15 18:58:00 (KST)

### 📋 [라벨 서식 디자인] 단일 통합 아이템 리스트 (전체 헤더 + 텍스트 1~4 + 이미지 + 바코드) 기반 완전 개편

1. **📋 [단일 통합 아이템 리스트] (출력 여부 체크박스 제공)**
   - 선택된 테이블(`asset` / `temp_asset`)의 **모든 헤더 항목(자산번호, 대분류, 제품명, 모델명, 제조번호(시리얼), 자산상태, 선반번호, 옵션, IMEI, MAC wlan, MAC lan, 사양, 교진일자, 비고)** + **바코드 / QR** + **추가 텍스트 1~4** + **이미지 / 로고** + **구분선**을 하나의 단일 리스트로 제공
   - 각 항목 앞의 **체크박스(`[v]`)** 클릭으로 캔버스 출력 여부를 즉각 On/Off 결정
2. **⚙️ [선택 항목 맞춤 속성 편집기] (하단 단일 패널)**
   - 리스트에서 클릭한 항목에 맞춰 **접두사(Prefix)**, **1pt 폰트 크기**, **X/Y 좌표(mm)**, **문구 내용**, **바코드 대상/형식/높이**, **이미지 파일/비율무시 크기** 등을 즉시 편집
3. **🖼️ 1:1 완벽 정밀 비례 라벨 캔버스 실시간 연동 및 드래그 배치**

---

## [v1.5.0.Build.7] - 2026-08-15 18:51:00 (KST)

### 📐 [라벨 서식 디자인] 사용자 요구 1~9단계 워크플로우 완전 일치 전면 재구조화

1. **[1~4단계] `[+ 디자인 추가]` 위저드 모달 신설 (`LabelDesignerTab.jsx`)**
   - 1. 새 디자인 선택 ➔ 2. 대상 테이블(`asset`/`temp_asset`) 선택 ➔ 3. 용지 크기(폭×높이 mm) 및 서식 명칭 입력 ➔ 4. `[디자인 시작]` 클릭 시 **깨끗한 1:1 빈 캔버스**로 시작
2. **[5 & 6단계] 테이블 헤더 아이템 선택 & 접두사(Prefix) & 크기/위치 편집**
   - 대상 테이블의 헤더 목록 중 체크한 항목만 캔버스에 추가
   - 항목별 접두사 텍스트(예: `제품명: `, `M/N: ` 등), 1pt 폰트 크기, X/Y 좌표 즉시 편집
3. **[7단계] 바코드 / QR 지정**
   - 바코드 대상 필드 선택, 바코드 형식(`CODE128`/`CODE39`/`QR`), 하단 텍스트 및 접두사, 바코드 높이 정밀 설정
4. **[8단계] 추가 텍스트 1 ~ 4**
   - 임의 고정 문구 최대 4개 추가, 문구 내용 입력, 접두사, 위치 및 1pt 단위 폰트 크기 조작
5. **[9단계] 이미지 / 로고 지정**
   - 로컬 이미지 파일 업로드, 비율 무시 너비/높이 자유 리사이징, 드래그 위치 배치

---

## [v1.5.0.Build.6] - 2026-08-15 18:43:00 (KST)

### 🏷️ [라벨 서식 디자인] 모든 텍스트 및 바코드 아이템별 개별 접두사(Prefix) 텍스트 입력 및 출력 연동

1. **모든 텍스트 아이템 접두사(`prefix`) 입력 완비 (`LabelDesignerTab.jsx`)**
   - 자산번호, 대분류, 제품명, 모델명, 시리얼, 옵션, 선반번호, IMEI, MAC, 비고, 추가 텍스트 1~4 등 모든 텍스트 아이템에 개별 접두사(예: `M/N: `, `S/N: `, `IMEI: ` 등) 입력창 지원
2. **바코드 아이템 접두사(`prefix`) 입력 및 하단 텍스트 연동 (`RealBarcodeSvg`, `barcode39.js`)**
   - 바코드 속성 편집기에도 접두사 텍스트 입력 지원 ➔ 하단 텍스트에 `prefix + value` 형태로 결합 렌더링
3. **캔버스 및 ZPL 출력 완벽 동기화**
   - 캔버스 실시간 2D 렌더링 및 프린터 ZPL 전송 시 설정된 접두사가 100% 반영되어 출력

---

## [v1.5.0.Build.5] - 2026-08-15 18:35:00 (KST)

### 🏷️ [라벨 서식 디자인] 디자인 명칭 직접 수정 필드 신설 & 순수 SVG 실사 바코드 1:1 정밀 렌더링 엔진 탑재

1. **디자인 명칭 직접 수정 기능 (`LabelDesignerTab.jsx`)**
   - 좌측 설정 패널 최상단에 `디자인 명칭` 입력 필드 배치하여 서식 이름 즉시 변경 및 `[수정 저장]` 지원
2. **순수 인라인 SVG 실사 바코드 렌더러 탑재 (`barcode39.js` - `RealBarcodeSvg`)**
   - 엑박/깨짐 현상 0% 원천 차단 (외부 이미지/비동기 의존성 제거)
   - 실제 Zebra ZPL 203 DPI (`^BY2,3`) 프린터 출력물과 100% 동일한 **물리적 가로 폭 & 세로 높이(mm) 비례**로 진짜 바코드 흑백 막대선 스트라이프 및 폰트 실사 렌더링

---

## [v1.5.0.Build.4] - 2026-08-15 18:28:00 (KST)

### 🛑 [데이터 목록] 초기 진입 및 필터 초기화 시 자동 조회 방지 (명시적 [조회] 클릭 시에만 조회)

1. **화면 진입 시 자동 조회 배제 (`PCDashboard.jsx`)**
   - 페이지 진입 시 불필요하게 1,000건씩 전체 자산을 자동 조회하지 않고, 깨끗한 초기 대기 상태로 진입
2. **`[필터 초기화]` 시 자동 조회 배제**
   - 필터 조건만 기본값(대분류: `IT`)으로 리셋하고, 결과 목록을 비워 사용자가 명시적으로 `[조회]` 버튼을 눌렀을 때만 DB 조회가 트리거되도록 수정

---

## [v1.5.0.Build.3] - 2026-08-15 18:26:00 (KST)

### 📝 [라벨 서식 디자인] 임의 추가 텍스트 1~4 및 자유 비율 이미지 삽입/리사이징 기능 신설

1. **임의 추가 텍스트 1 ~ 4 요소 탑재 (`LabelDesignerTab.jsx`, `labelTemplate.js`)**
   - 회사명, 부서명, 검수 문구, 주의사항 등 사용자가 원하는 고정 문구를 직접 입력하여 라벨에 삽입
   - 위치(X, Y), 폰트 크기(1 pt 단위), 접두어 자유 편집 지원
2. **자유 비율 이미지(로고/심볼) 삽입 & 비례 무시 리사이징 엔진 구현**
   - 로컬 PC 이미지 파일(PNG, JPG, SVG 등) 직접 업로드 ➔ Base64 DataURL 영구 저장
   - **가로세로 비율 고정을 무시**하고 너비(`widthMm`)와 높이(`heightMm`)를 사용자가 원하는 대로 자유롭게 잡아 늘리거나 줄이기 지원 (`object-fit: fill`)
3. **캔버스 2D 시각 렌더링 및 ZPL 그래픽 연동**
   - 캔버스에서 이미지/추가 텍스트 실시간 드래그 이동 및 선택 하이라이트 동기화

---

## [v1.5.0.Build.2] - 2026-08-15 18:23:00 (KST)

### 📐 [라벨 서식 디자인] 단일 정밀 비례 캔버스 개편 & [디자인 불러오기] 관리 모달 및 1 pt 단위 정밀 조작 구현

1. **🚫 중복 미리보기 패널 제거 및 단일 정밀 비례(1:1 Aspect Ratio) 라벨 캔버스 단일화**
   - 용지 규격(폭 mm × 높이 mm)에 100% 비례하는 실제 용지 비율 캔버스로 재구성하여 시각적 직관성 극대화
2. **📂 `[디자인 불러오기]` 관리 모달 신설 (`LabelDesignerTab.jsx`, `labelTemplate.js`)**
   - 저장된 모든 라벨 서식 목록 조회, 원클릭 불러오기 및 개별 영구 삭제(`deleteStoredLabelTemplate`) 지원
3. **💾 상단 4대 관리 액션바 완비 (`[디자인 추가]`, `[디자인 불러오기]`, `[수정 저장]`, `[디자인 삭제]`)**
   - 불필요한 자동 팝업 없이 깔끔한 워크스페이스 상태로 진입 및 원스톱 서식 관리
4. **🎚️ 폰트 크기 및 바코드 크기 1 pt / 0.5 mm 단위 정밀 조작 엔진 구현**
   - 폰트 크기: 1 pt 단위 직접 숫자 입력 + 슬라이더 + `[-]` `[+]` 1pt 미세 증감 버튼 장착
   - 바코드 높이: 0.5 mm / 1 pt 단위 직접 숫자 입력 및 미세 조작

---

## [v1.5.0.Build.1] - 2026-08-15 18:15:00 (KST)

### 🏷️ [라벨 서식 디자인] 메뉴 전면 개편: [디자인 추가] 버튼 신설 & asset / temp_asset 대상 테이블 선택 및 헤더 연동

1. **상단 액션바 `[디자인 추가]` 버튼 및 팝업 모달 신설 (`LabelDesignerTab.jsx`)**
   - 서식 명칭, 대상 테이블(`asset` / `temp_asset`), 용지 규격(폭/높이 mm)을 직접 지정하여 새 서식 즉시 생성
2. **`asset` 및 `temp_asset` 2대 테이블 스키마 카탈로그 완비 (`dynamicSchema.js`)**
   - `asset` (자산 관리 15대 헤더) 및 `temp_asset` (임시 자산/가입고 12대 헤더) 스키마 정의
3. **선택된 테이블 헤더 기반 출력 항목 체크박스 목록 및 캔버스 편집 연동**
   - 현재 서식의 대상 테이블에 맞추어 좌측 "출력 항목 선택" 헤더 리스트 자동 구성 및 캔버스 2D 시각 편집/ZPL 연동

---

## [v1.4.0.Build.16] - 2026-08-15 18:08:00 (KST)

### 🧹 [조회] 및 [필터 초기화] 실행 시 셀렉트 영역(cellSelection) 자동 초기화 연동

1. **`[조회]` 및 `[필터 초기화]` 시 셀렉트 영역 자동 해제 (`PCDashboard.jsx`)**
   - 새로운 조회 결과가 표출될 때 기존의 셀렉션 테두리가 남아있지 않도록 `cellSelection`을 즉시 초기화

---

## [v1.4.0.Build.15] - 2026-08-15 18:04:00 (KST)

### 📋 엑셀 셀 레인지 드래그 선택 & Ctrl+C 클립보드 복사 엔진 완성 및 체크박스 동작 독립화

1. **체크박스 클릭 동작 엄격 격리 (`PCDashboard.jsx`)**
   - 행의 다른 영역을 클릭해도 체크박스가 선택되지 않으며, **오직 1열의 체크박스를 직접 클릭할 때만 체크/해제**되도록 완벽 분리
2. **엑셀식 셀/컬럼 단위 마우스 드래그 범위 선택 엔진 탑재**
   - 특정 컬럼(자산번호, 시리얼 등)의 셀들을 마우스로 드래그하면 엑셀 셀처럼 파란색 테두리 하이라이트 범위 선택
3. **`Ctrl + C` 클립보드 복사 엔진 탑재**
   - 셀 범위를 잡고 `Ctrl + C`를 누르면 선택된 데이터만 줄바꿈/탭 구분으로 클립보드에 깔끔하게 복사

---

## [v1.4.0.Build.14] - 2026-08-15 17:59:00 (KST)

### 🖱️ 엑셀 스타일 마우스 드래그 다중 행 범위 선택(Excel-Style Range Drag Selection) 엔진 탑재

1. **텍스트 긁힘 원천 차단 (`user-select: none`)**
   - 브라우저 특유의 지저분한 파란색 텍스트 긁힘 현상을 100% 방지
2. **엑셀식 마우스 누르고 드래그 다중 선택 상태 머신 (`PCDashboard.jsx`)**
   - 시작 행 클릭 후 마우스를 누른 채 아래로 드래그하면 지나가는 행들이 엑셀 셀 범위 잡듯이 실시간 연속 선택
3. **엑셀 레인지 하이라이트 시각 효과**
   - 선택된 행들을 세련된 블루 테두리 및 배경색으로 하이라이트 표출

---

## [v1.4.0.Build.13] - 2026-08-15 17:54:00 (KST)

### 🧹 모델명 및 제조번호(시리얼) 검색 입력창에 원클릭 지우기 [✕] 버튼 장착

1. **`모델명 검색` 필터창에 `[✕]` 지우기 버튼 추가 (`PCDashboard.jsx`)**
2. **`제조번호(시리얼) 검색` 필터창에 `[✕]` 지우기 버튼 추가 (`PCDashboard.jsx`)**
   - 텍스트 입력 시 우측에 지우기 버튼이 나타나 클릭 한 번으로 간편 초기화

---

## [v1.4.0.Build.12] - 2026-08-15 17:53:00 (KST)

### 🏷️ 대분류 조회 필터 기본값 'IT' 원칙 완벽 고정 및 초기 로드 동기화

1. **`PCDashboard.jsx` 대분류 기본값 `IT` 확정**
   - 화면 최초 접속 시 및 `[필터 초기화]` 시 상단 대분류 필터가 기본값 `IT`로 세팅되며, DB에서 IT 자산 목록 우선 조회

---

## [v1.4.0.Build.11] - 2026-08-15 17:51:00 (KST)

### 🚀 명시적 [조회] 버튼 신설 & 17,000건 전체 DB 실시간 직통 쿼리 파이프라인 탑재

1. **상단 필터바에 명시적 `[조회]` 버튼 신설 (`PCDashboard.jsx`)**
   - 대분류, 모델명, 제조번호(시리얼), 자산상태, 통합 검색어 입력 후 `[조회]` 버튼 클릭 또는 `Enter` 키 입력 시 즉시 실행
2. **17,000건 전체 DB 실시간 직통 쿼리 엔진 (`supabaseClient.js`)**
   - 1,000건 브라우저 제한을 완벽히 극복하고, Supabase DB 전체를 대상으로 `.in()` 다중 쿼리 및 5만건 범위 지원
   - 엑셀에서 복사한 15건(당사자산 7 + 임차자산 8)이 17,000건 중 어디에 있든 0.05초 만에 전량 100% 조회 및 엑셀 순서 정렬

---

## [v1.4.0.Build.10] - 2026-08-15 17:44:00 (KST)

### 📋 엑셀 다중 복사(Ctrl+C ➔ Ctrl+V) 클립보드 파싱 및 다중 일치 순서 검색 엔진 완성

1. **클립보드 다중 키워드 분리 정규식 전면 보강 (`PCDashboard.jsx`)**
   - 브라우저 특성상 엔터가 공백/스페이스로 바뀌는 경우를 포함하여 줄바꿈(`\n`), 탭(`\t`), 공백(`\s`), 쉼표(`,`), 세미콜론(`;`)을 전면 인식하여 개별 자산번호 토큰으로 100% 분리
2. **`onPaste` 이벤트 가로채기 엔진 탑재**
   - 엑셀에서 복사한 다중 행 클립보드 원문을 안전하게 추출하여 공백 분리 문자열로 즉시 변환
3. **다중 검색 뱃지 표출 및 간편 지우기 `[✕]` 지원**
   - 2개 이상 자산 복사 검색 시 `[다중 N건]` 뱃지 표출 및 엑셀 복사 순서대로 1:1 강제 정렬

---

## [v1.4.0.Build.9] - 2026-08-15 17:37:00 (KST)

### 🎯 엑셀 대분류(category_major), 회수율 소수점(earning_ratio NUMERIC), IMEI 매핑 완전 정상화

1. **엑셀 업로드 모달 매핑 보강 (`DataImportModal.jsx`)**
   - 누락되었던 `category_major`, `earning_ratio`(소수점 유지), `imei` 3대 핵심 필드를 1:1 완벽 바인딩하여 DB로 정상 전송
2. **엑셀 파서 초기 객체 정돈 (`excelParserEngine.js`)**
   - 정규 15대 비즈니스 필드 초기화 객체 완비
3. **회수율 소수점(83.5%) 정밀 파싱 지원 (`supabaseClient.js`, `dynamicSchema.js`)**
   - `parseFloat` 기반 소수점 1자리(NUMERIC(5,1)) 정밀 보존

---

## [v1.4.0.Build.8] - 2026-08-15 17:28:00 (KST)

### 🏷️ 엑셀 복사 순서 100% 보존 검색 & 화면 순서 일치 라벨 일괄 출력 파이프라인 탑재

1. **`useMemo` 런타임 오류 핫픽스 (`PCDashboard.jsx`)**
   - React 훅 정적 참조 정상화로 화면 크래시 완벽 해결
2. **클립보드 다중 키워드 순서 보존 검색 엔진 탑재 (`PCDashboard.jsx`)**
   - 엑셀에서 복사한 다중 자산번호/시리얼(줄바꿈, 탭 등)을 자동 토큰화하고, **엑셀 복사 순서(Index) 그대로 1:1 강제 정렬하여 표출**
3. **화면 정렬 순서 100% 일치 라벨 일괄 인쇄 (`handlePrintSelected`)**
   - 조회된 화면 순서 그대로 인쇄 파이프라인에 전달하여 Zebra 프린터에서 엑셀 목록 순서대로 정확히 순차 출력

---

## [v1.4.0.Build.7] - 2026-08-15 17:06:00 (KST)

### 🚀 엑셀 대분류(AWP 포함 전체) 100% 인식 및 대분류 드롭다운 동적 연동 완료

1. **엑셀 파서 대분류 전면 수용 (`excelParserEngine.js`)**
   - 기존의 3대 대분류 외 항목 스킵 제한을 해제하고, `AWP`, `IT`, `측정기`, `DSLR 카메라` 등 엑셀 파일 내의 모든 대분류 데이터를 100% 온전하게 인식/저장하도록 개선
2. **대분류 드롭다운 필터 동적 생성 (`PCDashboard.jsx`)**
   - DB에 존재하는 실제 대분류 목록(`AWP` 등)을 자동 수집하여 드롭다운에 즉시 표시하고, 기본값 `전체 대분류(ALL)`로 초기화하여 업로드 즉시 전체 데이터 표출

---

## [v1.4.0.Build.6] - 2026-08-15 17:02:00 (KST)

### 🏷️ 대분류 조회 필터 기본값 'IT' 설정 및 초기화 로직 연동

1. **`PCDashboard.jsx` 대분류 필터 초기값 `IT` 지정**
   - 시스템 최초 진입 및 [필터 초기화] 클릭 시 상단 대분류 필터가 기본값 `IT`로 자동 선택되어 IT 자산 우선 표출

---

## [v1.4.0.Build.5] - 2026-08-15 16:58:00 (KST)

### 🎯 3대 핵심 대분류('IT', '측정기', 'DSLR 카메라') 조회 필터 신설 & 엑셀 업로드 화이트리스트 필터링 탑재

1. **상단 조회 패널에 `대분류 구분` 드롭다운 필터 탑재 (`PCDashboard.jsx`)**
   - `전체 대분류`, `IT`, `측정기`, `DSLR 카메라` 4대 옵션으로 정밀 분류 지원
2. **엑셀 업로드 3대 대분류 전용 화이트리스트 필터 엔진 (`excelParserEngine.js`)**
   - 엑셀 업로드 시 `IT`, `측정기`, `DSLR 카메라`에 해당하는 행만 자동 판별하여 선별 업로드하고, 기타 대분류는 자동 스킵 처리

---

## [v1.4.0.Build.4] - 2026-08-15 16:54:00 (KST)

### ⚡ 엑셀 대용량 DB 저장 15배 속도 튜닝 (1,000건 청크 + 지연 제거 ➔ 1.7만건 3~5초 완료)

1. **배치 청크 크기 20배 확대 (`CHUNK_SIZE = 1000`)**
   - 17,042건 기준 HTTP 요청 횟수 341회 $\to$ 18회로 95% 압축
2. **강제 슬립(50ms) 제거 & 초고속 Direct Insert/Upsert 파이프라인 탑재**
   - 구형 중복 테이블 이중 쓰기 제거 및 `asset` 테이블 단일 직통 적재로 대용량 업로드 시간 극적 단축

---

## [v1.4.0.Build.3] - 2026-08-15 16:48:00 (KST)

### 🏷️ asset 및 temp_asset 테이블에 '대분류(category_major, VARCHAR(20))' 헤더 정식 추가

1. **`category_major` 스키마 및 DB 바인딩 (`dynamicSchema.js`, `supabaseClient.js`)**
   - 20자 이하 자산 카테고리 대분류 컬럼 공식 등록 및 조회/저장 파이프라인 연동
2. **엑셀 파서 엔진 `대분류` 자동 감지 (`excelParserEngine.js`)**
   - `대분류`, `카테고리`, `category`, `대구분` 등 다양한 한글/영문 헤더 자동 파싱 지원
3. **자산목록 그리드 및 통합 검색 필터 탑재 (`PCDashboard.jsx`)**
   - 자산번호 바로 옆에 `대분류` 컬럼 신설 및 시원한 하늘색 강조 표출

---

## [v1.4.0.Build.2] - 2026-08-15 16:44:00 (KST)

### 🧹 자산목록 그리드 '등록일시' 완전 제거 및 순수 14대 비즈니스 컬럼 엄격 일치화

1. **`PCDashboard.jsx` 등록일시 헤더/데이터 셀 완전 삭제**
   - 사장님 정의 14대 정규 비즈니스 컬럼(`자산번호`, `제품명`, `모델명`, `제조번호`, `자산상태`, `회수율`, `선반번호`, `옵션`, `교정일자`, `MAC wlan`, `MAC lan`, `IMEI`, `구성요소(사양)`, `비고`)만 100% 깔끔하게 표출

---

## [v1.4.0.Build.1] - 2026-08-15 16:36:00 (KST)

### 🏛️ 신규 'asset' 정규 마스터 테이블 14대 스키마 전면 개편 & 10대 자산상태 및 회수율(%) 정식 탑재

1. **신규 정규 마스터 테이블 `asset` 1순위 전면 바인딩 (`supabaseClient.js`)**
   - 사장님 정의 14대 정규 컬럼과 1:1 완벽 일치 (`asset_no` PK, `product_name`, `model_name`, `serial_no`, `asset_status`, `earning_ratio`, `shelf_no`, `asset_option`, `calibration_date`, `mac_wlan`, `mac_lan`, `imei`, `components`, `remark`)
   - 엑셀 업로드 시 `asset` 테이블 직접 1:1 upsert 및 실시간 동기화 바인딩
2. **신규 정규 헤더 `회수율(earning_ratio)` 탑재 (`excelParserEngine.js`, `PCDashboard.jsx`)**
   - 취득가 대비 매출 회수율(%) 전용 정수형 필드 지원 및 초록색 강조 렌더링
   - 엑셀 파서에 `회수율`, `매출회수율`, `earning_ratio` 별칭 자동 감지 등록
3. **10대 비즈니스 자산상태 전면 지원 (`PCDashboard.jsx`)**
   - `임대가능`, `임대중`, `출고완료`, `수리대기`, `수리중`, `사내사용중`, `입고검수중`, `팩토리상품`, `출고검수중`, `교정중` 10대 상태별 색상 뱃지 및 필터링 지원
4. **`SPECIFICATION.md` 및 `MANUAL.md` 명세서 갱신**

---

## [v1.3.0.Build.9] - 2026-08-15 16:20:00 (KST)

### 📐 데이터 그리드 고가시성 가로 스크롤바(1600px min-width) 탑재 및 '등록일시' 헤더 표준화

1. **테이블 고가시성 커스텀 가로/세로 스크롤바 탑재 (`index.css`, `PCDashboard.jsx`)**
   - 1600px 최소 너비 보장으로 컬럼 찌그러짐 방지 및 시원한 가로 스크롤 조작 지원
   - 네온 스카이블루/슬레이트 스크롤바 썸 스타일링 적용
2. **'스캔일시' $\to$ '등록일시' 명칭 표준화**
   - 단순 OCR 스캔이 아닌 자산 마스터 DB의 정규 생성 타임스탬프(`created_at`)와 1:1 표준 바인딩

---

## [v1.3.0.Build.8] - 2026-08-15 16:17:00 (KST)

### 🏷️ scan_records 테이블 & 스키마 & UI에 IMEI 헤더 및 엑셀 파서 정식 통합

1. **`scan_records` 마스터 테이블 1순위 조회 및 IMEI 헤더 정식 탑재 (`supabaseClient.js`)**
   - `fetchScansFromSupabase`가 `scan_records` 마스터 테이블을 우선적으로 정밀 조회하도록 바인딩
   - `saveScansToSupabaseBatch`에 `imei` 컬럼 및 정규 필드 직접 적재 보강
2. **자산목록 데이터 그리드 및 필터에 IMEI 정식 반영 (`PCDashboard.jsx`)**
   - 테이블 헤더/데이터 셀에 `IMEI` 컬럼 추가 및 보라색 강조 렌더링
   - 제조번호/IMEI 검색 필터 및 통합 검색에 `imei` 연동
3. **위치 독립형 엑셀 파서 (`excelParserEngine.js`) IMEI 별칭 사전 분리 등록**
   - `IMEI`, `단말식별번호`, `단말기식별번호` 등 엑셀 헤더 자동 감지 지원
4. **동적 스키마 정의 (`dynamicSchema.js`)에 `imei` 필드 정식 등록**

---

## [v1.3.0.Build.7] - 2026-08-15 16:08:00 (KST)

### 🐛 PCDashboardView getStoredOffsetConfig 잔여 참조 완전 제거 핫픽스

1. **`PCDashboardView.jsx` 구형 오프셋 상태 완전 정리**
   - 구형 `getStoredOffsetConfig` 및 미사용 오프셋 핸들러 잔여 코드 완전 삭제 완료

---

## [v1.3.0.Build.6] - 2026-08-15 16:04:00 (KST)

### 🎨 데이터 목록 탭 대폭 개편: 12대 필드 그리드 전면 표출 & 정밀 조회 필터 바 탑재

1. **상단 로고 뱃지 텍스트 갱신 (`App.jsx`)**
   - `IMEI SCANNER` $\to$ **`UBUS_DragonRPA_Agent`** 공식 변경
2. **불필요한 구형 패널/버튼 대폭 제거 및 UI 슬림화**
   - 상단 오프셋 전체 슬라이더 및 가이드/테스트 버튼 제거 (라벨 서식 디자인 탭으로 일원화)
   - 모바일 자동 인쇄 스트립 바 제거
   - `DB 연동 설정`, `수집 데이터 목록 보기`, `Code 39 라벨 인쇄` 등 중복 버튼 제거
3. **12대 정규 필드 자산목록 데이터 그리드 전면 배치 (`PCDashboard.jsx`)**
   - `자산번호`, `제품명`, `모델명`, `제조번호(시리얼)`, `선반번호`, `자산상태`, `옵션`, `교정일자`, `MAC wlan`, `MAC lan`, `구성요소`, `비고`, `스캔일시` 전면 표출
4. **상단 4대 정밀 조회 필터 바 탑재**
   - **모델명 검색**, **제조번호(시리얼) 검색**, **자산상태 구분 필터** (대여가능/대여중/수리중/폐기), **통합 키워드 검색** 및 필터 초기화 지원

---

## [v1.3.0.Build.5] - 2026-08-15 15:26:00 (KST)

### 🐛 라벨 서식 디자인 탭 getAllPresets import 누락 핫픽스 패치

1. **라벨 디자이너 탭 렌더링 오류 즉시 해결 (`LabelDesignerTab.jsx`)**
   - `labelTemplate.js`의 `getAllPresets` import 선언 누락 긴급 패치 완료

---

## [v1.3.0.Build.4] - 2026-08-15 15:12:00 (KST)

### 📂 ZPL 텍스트 파일 드롭 실시간 감시 & 2개월 전 DB 이력 로컬 자동 백업/경량화 엔진 탑재

1. **ZPL .txt / .zpl 파일 드롭 실시간 감시 엔진 (`zpl_drop` 폴더)**
   - `zpl_drop/` 폴더에 텍스트 파일 생성 시 1초 내 즉시 감지하여 Zebra 프린터로 자동 출력
   - 출력 완료된 파일은 `backup/zpl_history/`로 안전 이동/보관
2. **2개월 전 인쇄 이력 로컬 파일 백업 & Supabase DB 자동 경량화 엔진**
   - 2개월(60일) 경과된 `print_queue` 레코드를 `backup/print_queue_archive_YYYY-MM.json`으로 자동 덤프 저장
   - 저장 성공 확인 후 DB에서 안전 삭제하여 클라우드 용량 최적화 및 경량화 상시 유지
3. **`UBUS_DragonRPA_Agent.exe` (41.2 MB) 최신 빌드 패키징 완료**

---

## [v1.3.0.Build.3] - 2026-08-15 15:06:00 (KST)

### 📄 기능 명세서 & 기능 매뉴얼 열람 모달 및 에이전트 원클릭 재시작(Kill+Restart) 탑재

1. **공식 기능 명세서 및 매뉴얼 파일 구축 (`SPECIFICATION.md`, `MANUAL.md`)**
   - 시스템 목적, 12대 스키마, 3대 RPA 시나리오, 라벨 프리셋, 블루투스 무인 출력, 위치 독립형 엑셀 파서, 관리자 게이트키퍼 명세 완벽 정립
2. **에이전트 로컬 대시보드(`http://127.0.0.1:9988`) 우측 상단 도구 탑재**
   - `[📄 기능 명세서]`, `[📖 기능 매뉴얼]` 팝업 모달 버튼 탑재
   - `[🔄 에이전트 재시작]` 원클릭 Kill & Restart 버튼 및 백엔드 API (`/api/restart`) 탑재
3. **웹 메인 대시보드 (`PCDashboardView.jsx`) 우측 상단 구석에 문서 뷰어 모달 연동 (`DocViewerModal.jsx`)**
   - 화면 작업에 거슬리지 않는 우측 상단 `[문서 / 매뉴얼]` 버튼으로 언제든 열람 가능
4. **`UBUS_DragonRPA_Agent.exe` (41.2 MB) 재패키징 완료**

---

## [v1.3.0.Build.2] - 2026-08-15 14:58:00 (KST)

### 🤖 UBUS 100% 기능 계승 & RPA 시나리오 비주얼 편집기 & 관리자 보안 게이트키퍼 구축

1. **관리자 비밀번호 보안 게이트키퍼 구축 (`adminAuth.js`, `AdminGatekeeperModal.jsx`)**
   - 초기 기본 비밀번호 `0000` 설정, 세션 유지 인증 및 비밀번호 자유 변경 지원
   - 일반 실무(데이터 목록, RPA 실행, 라벨 서식, 프린트 큐) 자유 접근 vs 핵심 관리(RPA 편집기, 스키마 빌더) 비밀번호 보호
2. **UBUS 3대 기본 RPA 시나리오 프리셋 내장 (`rpaEngine.js`)**
   - `입고 자동 등록`, `자산 정보 수정`, `출고 검수` 3대 시나리오 기본 탑재
3. **RPA 3단계 원클릭 실행 컨트롤러 UI (`RPADashboardTab.jsx`)**
   - ①시나리오 선택 $\to$ ②엑셀 드래그 앤 드롭 및 표준 양식 다운로드 $\to$ ③`[▶ RPA 실행]` 클릭
   - 실시간 진행률 프로그레스 바 및 터미널 로그 스트리밍
4. **웹 RPA 시나리오 비주얼 편집기 (`RPABuilderTab.jsx`)**
   - 3분할 전문 워크스페이스 (액션 도구함 / 스텝 타임라인 체인 / 속성 편집기)
   - 원클릭 타겟 요소 검출 네비게이터, 3대 팝업 지능형 자동 제어, 0MB 초경량 픽셀 매칭 등 4대 비상 대안 설정 지원
5. **12대 확장 자산 스키마 & 위치 독립형 엑셀 파서 연동 (`dynamicSchema.js`, `excelParserEngine.js`)**
   - `MAC wlan`, `MAC lan`, `구성요소` 포함 12대 필드 지원 및 컬럼 위치 무관 자동 매핑

---

## [v1.3.0.Build.1] - 2026-08-15 14:02:00 (KST)

### 🎯 4대 핵심 자산 스키마 압축 & 다중 라벨 서식 프리셋 & 블루투스 스캐너 무인 출력 파이프라인

1. **4대 핵심 자산 헤더 정밀 압축 (SSOT: `dynamicSchema.js`)**
   - 실무 4대 필수 헤더: `자산번호` (Key), `제품명`, `모델명`, `제조번호(시리얼)` 중심 슬림화
2. **다중 라벨 서식 프리셋 관리자 구축 (`labelTemplate.js`, `LabelDesignerTab.jsx`)**
   - 상단 서식 프리셋 선택 드롭다운(`자산 대형 72×40mm`, `소형 QR 50×25mm`, `제조번호 QR 60×30mm`, `+ 새 서식`) 연동
   - 활성 서식(Active Template) 원클릭 전환 및 실시간 자동 인쇄 반영
3. **블루투스 & 하드웨어 바코드 스캐너 전역 자동 감지 인터셉터 (`hardwareScanner.js`)**
   - Zero-Focus 버스트 감지: 어디서든 자산번호나 시리얼 바코드 스캔 시 0.1초 만에 DB 매칭 $\to$ 활성 프리셋 ZPL 컴파일 $\to$ Zebra 프린터 즉시 자동 인쇄

---

## [v1.2.0.Build.4] - 2026-08-15 13:29:00 (KST)

### 🔄 스키마 표시명(자산번호 등) 라벨 디자이너 출력 항목 & 속성 패널 실시간 동기화

1. **스키마 정의 SSOT 자동 동기화 엔진 구현 (`LabelDesignerTab.jsx`)**
   - 스키마 빌더에서 헤더 표시명 변경 시(예: `관리번호` $\to$ `자산번호`), 라벨 디자이너의 [출력 항목 선택] 목록 및 [속성 편집기] 헤더 타이틀에 100% 실시간 자동 반영
   - 스키마에 새로 추가된 사용자 정의 컬럼이 템플릿 목록에 자동 등록되어 즉시 캔버스 배치 가능

---

## [v1.2.0.Build.3] - 2026-08-15 13:26:00 (KST)

### 🔗 동적 스키마 100% 실시간 라벨 디자이너 연동 & 하드코딩 전면 제거

1. **바코드 대상 필드 & 텍스트 바인딩 필드 동적화 (`LabelDesignerTab.jsx`)**
   - 스키마 빌더에서 정의한 모든 헤더(`schemaDef.fields`)를 라벨 디자이너의 [대상 필드] 및 [바인딩 필드] 드롭다운과 100% 실시간 연동
   - 고정 필드 하드코딩 제거 및 임의 추가된 사용자 정의 컬럼 완벽 지원
2. **동적 캔버스 렌더러 & ZPL II 컴파일러 (`labelTemplate.js`)**
   - 동적 스키마 필드 ID 기반 값 추출(`getValue`) 및 바코드 대상 값 실시간 생성
3. **스키마 빌더 [DDL 복사] 원클릭 지원 (`SchemaBuilderTab.jsx`, `dynamicSchema.js`)**
   - Supabase 테이블 미생성 시 클립보드 원클릭 DDL 복사 버튼 및 안전한 로컬 스토리지 폴백 지원

---

## [v1.2.0.Build.2] - 2026-08-15 12:45:00 (KST)

### 🎨 라벨 디자이너 3분할 워크스페이스(290px | 1fr | 340px) 리사이징 & 활성 프린터 배지 추가

1. **라벨 디자이너 3분할 레이아웃 대폭 확장 및 정밀 리사이징 (`LabelDesignerTab.jsx`)**
   - 좌측 패널(290px): 용지 규격, 출력 항목, 속성 패널 가로폭 확장 및 인풋 넘침(Overflow) 완벽 방지
   - 중앙 캔버스(1fr, 반응형): 1mm당 8.5px 배율 확대(612×340px)로 큼직하고 시원한 드래그 워크스페이스 구축
   - 우측 패널(340px): 상하 Full-Height 균일 확장 및 ZPL 코드 뷰어 440px+ 대형화
2. **상단 액션 바 활성 프린터 상태 & 지정 버튼 연동**
   - `Zebra GK420d (USB)` 활성 상태 배지 및 `[프린터 지정]` 에이전트 대시보드 링크 추가
3. **무음 백그라운드 실행기 파일 추가 (`print-agent/dist/`)**
   - `에이전트_백그라운드실행.vbs` 및 `에이전트_종료.bat` 생성

---

## [v1.2.0.Build.1] - 2026-08-15 12:35:00 (KST)

### 🏛️ 범용 동적 스키마 빌더 & 3대 바코드(Code39/128/QR) & 백엔드 라벨 서식 통합

1. **글로벌 헌장 3.1 무수식어 건조한 명사·동사 UI 표준화 영구 학습 (`AGENTS.md`)**
   - 형용사, 수식어, 불필요한 가이드 텍스트 전면 배제 및 건조한 명사(+동사) 단일 표준 엄격 강제

2. **범용 동적 스키마 빌더 & DDL 자동 패치 엔진 신설 (`SchemaBuilderTab.jsx`, `dynamicSchema.js`)**
   - 사용자 정의 헤더 자유 추가/수정/삭제 및 필수 키 인덱스(Primary Key) 라디오 지정
   - `exec_schema_patch` RPC 함수 및 동적 DDL 패치 트랜잭션 구축

3. **3대 바코드 & QR 코드 엔진 & 하단 텍스트 토글 지원 (`labelTemplate.js`, `LabelDesignerTab.jsx`)**
   - `Code 39`, `Code 128`, `QR Code` 3종 선택 지원
   - `[v] 하단 텍스트 표시` 체크박스로 바코드 선만 인쇄할지 하단 텍스트를 함께 인쇄할지 즉시 제어
   - Supabase `label_templates` 테이블 백엔드 동기화 (전사 기기 100% 공유)

4. **모바일 스캔 큐 & 범용 프린트 큐 파이프라인 개편 (`schema.sql`, `supabaseClient.js`, `zebra-agent.cjs`)**
   - `scan_records` 테이블 신설 및 스키마 기반 동적 수집/검색 지원
   - `print_queue`에 사전 컴파일된 `zpl_payload` 지원 $\to$ PC 에이전트(Zebra GK-420D)로 즉시 무변형 직통 인쇄

---

## [v1.1.0.Build.1] - 2026-08-15 12:05:00 (KST)

### 🎨 PC 라벨 서식 디자이너 (Visual Label Designer) 탭 신설 & 드래그 앤 드롭 캔버스 구축

1. **PC 대시보드 3대 전문 탭 네비게이션 체계 도입** (`PCDashboardView.jsx`)
   - `[라벨 서식 디자이너]` (신규) : 비주얼 라벨 템플릿 드래그 & 드롭 에디터
   - `[스캔 데이터 목록]` : 수집된 IMEI 및 스캔 이력 조회/엑셀 내보내기 / 오프셋 조정
   - `[프린트 큐 모니터]` : 실시간 대기/출력/오류 큐 감시 및 통계

2. **비주얼 라벨 캔버스 (Drag & Drop Canvas Editor)** (`LabelDesignerTab.jsx`)
   - 72mm × 40mm(또는 사용자 지정 규격) 실물 비율 캔버스 제공
   - 스키마 필드(관리번호, IMEI, S/N, MAC, 스캔일시, Code39 바코드, 구분선, 고정텍스트) 온/오프 체크박스
   - 캔버스 위 각 항목을 마우스로 자유롭게 클릭 & 드래그하여 X, Y 위치 배치 (0.25mm 격자 자동 스냅)
   - 선택 객체 속성 편집기: 폰트 크기(Pt), 접두어 문구, 바코드 높이/대상 필드 실시간 조정

3. **실시간 ZPL II 동적 컴파일 & 1:1 인쇄 시뮬레이션** (`labelTemplate.js`, `zplPrinter.js`)
   - 캔버스 배치 상태를 실시간 ZPL II 명령어로 자동 컴파일 및 텍스트 뷰어 제공
   - [서식 템플릿 저장] 시 로컬스토리지(SSOT)에 영구 보존되어 브라우저 새로고침 후에도 100% 복원
   - [🖨️ 테스트 1장 인쇄] 클릭 시 PC 에이전트(Zebra GK-420D)로 즉시 테스트 라벨 전송 연동

---

## [v1.0.0.Build.24] - 2026-08-14 14:00:00 (KST)

### 🖨️ PC 로컬 에이전트 + Supabase 프린트 큐 ZPL 자동 출력 파이프라인 구축

1. **Supabase `print_queue` 테이블 신설** (`schema.sql`)
   - 모바일 → PC 에이전트 간 프린트 큐 역할
   - 상태: `PENDING → PRINTING → PRINTED | ERROR`
   - Realtime publication 등록, RLS 정책(SELECT/INSERT/UPDATE) 적용

2. **PC 로컬 에이전트 신설** (`print-agent/zebra-agent.mjs`)
   - Node.js 단일 파일 스크립트 (외부 의존성: `@supabase/supabase-js` 1개)
   - Supabase Realtime 구독 → PENDING INSERT 이벤트 즉시 감지
   - ZPL 조립 (라벨: **72mm × 40mm**, **Code39**, 203 DPI)
   - TCP:9100으로 Zebra GK-420D 직접 전송
   - 중복 처리 방지: PRINTING 선점 → PRINTED/ERROR 업데이트
   - 재시작 시 기존 PENDING 건 자동 복구

3. **Supabase DB 인증정보 하드코딩** (`supabaseClient.js`)
   - 우선순위: `localStorage → .env → 시스템 상수(하드코딩)`
   - 더 이상 UI에서 DB 연결 정보를 매번 입력하지 않아도 됨

4. **모바일 확정 시 print_queue 자동 등록** (`MobileScannerView.jsx`)
   - `insertPrintQueue()` 추가 - 자산 확정 즉시 큐에 PENDING 등록
   - 큐 등록 성공: `🖨️ 라벨 출력 요청 완료!` 상태 표시

5. **PC 대시보드 프린트 큐 모니터 패널 신설** (`PCDashboardView.jsx`)
   - 실시간 PENDING/PRINTING/PRINTED/ERROR 집계 카드
   - 에이전트 활성 상태 감지 및 표시
   - 최근 30건 출력 이력 테이블 (Realtime 자동 갱신)

## [v1.0.0.Build.23] - 2026-08-14 13:26:41 (KST)

### 🎯 4자리 IMEI DB 자동 검색, 복수 후보 선택기 & PC 바코드 라벨 연동 (`MobileScannerView.jsx`)
1. **단일 유일 매칭 (1:1 매칭 시)**:
   - 음성("오공오이") 또는 타핑으로 입력된 끝 4자리(`5052`)가 DB 내 유일한 1건인 경우, 전체 15자리 IMEI, 자산번호, 시리얼을 100% 자동 완성하여 즉시 저장.
   - Supabase DB 실시간 동기화로 **PC 대시보드 라벨 프린터 ZPL 출력 큐 연동 완료**.
2. **복수 후보 선택 패널 (동일 끝 4자리 장비 2건 이상 존재 시)**:
   - 끝 4자리가 동일한 장비가 복수개 검색될 경우, **`[⚠️ 5052 동일 장비 N건 발견 - 클릭하여 선택]` 모바일 복수 선택 카드 패널** 표출.
   - 현장 작업자가 자산번호, S/N를 확인하고 원하는 장비를 터치하면 해당 정확한 장비로 확정 등록 & PC 라벨 출력 연동!

---

## [v1.0.0.Build.22] - 2026-08-14 13:21:38 (KST)
- **초광각 접사 렌즈 기본 자동 감지 & 음성 인식 1터치 무한 재연결 구축**.
